

walker Tourist {
    can travel with City entry {

        def foo(a:int){}
        x = lambda a: int, b: int : b + a;
        y = lambda : 567;

    }
    def visit_city(c:City){
        print("Visiting", c.name);
    }
}
