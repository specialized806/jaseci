# JSX Elements with Contextual Lexing

with entry {
    # JSX elements with contextual lexing
    let element = <div>Hello</div>;
    let component = <Component prop="value" />;
    let nested = <Parent><Child /></Parent>;
}
